import { Html, Head, Main, NextScript } from "next/document";

export default () => (
	<Html lang="en">
		<Head />
		<body className="text-white" style={{ background: "#262626" }}>
			<Main />
			<NextScript />
		</body>
	</Html>
);
